<form method="post" action="{{ path('app_ticket_delete', {'id_ticket': ticket.id_ticket}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ ticket.id_ticket) }}">
    <button class="btn">Delete</button>
</form>
