<form method="post" action="{{ path('app_back_delete', {'id_res': reservation.id_res}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ reservation.id_res) }}">
    <button class="btn btn-danger" type="submit" style="width:100%">Delete</button>
</form>
